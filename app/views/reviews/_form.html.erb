<%= form_for [@restaurant, @review] do |f| %>
  <%= f.text_field :content %>
  <%= f.label :content %>
  <%= f.text_field :rating %>
  <%= f.label :rating %>
  <%= f.submit "add a review" %>
<% end %>
